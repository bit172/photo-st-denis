{
  "name": "@photo-st-denis/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for photo management system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist node_modules",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "test": "jest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
